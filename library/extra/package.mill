package build.extra

import mill.*
import mill.javalib.*
import mill.javalib.publish.*
import mill.scalajslib.*
import mill.scalajslib.api.*
import mill.scalalib.*
import millbuild.*

object `package` extends Cross[ExtraModule]("2.13.17", "3.3.4")
trait ExtraModule extends LibraryPublishModule {

  def mvnDeps = super.mvnDeps() ++ Seq(Deps.compileTime) ++
    (scalaVersion() match {
      case "2.13.17" => Seq(Deps.scalaReflect)
      case _         => Nil
    })

  def compileMvnDeps = super.compileMvnDeps() ++
    (scalaVersion() match {
      case "2.13.17" => Seq(Deps.scalaCompiler)
      case _         => Nil
    })

  def moduleDeps = super.moduleDeps ++ Seq(build.coreGeneric())

  def compileModuleDeps = super.compileModuleDeps ++
    Seq(build.utilDummyDefaults())

  def artifactName = "extra"

  def scalacOptions = super.scalacOptions() ++
    (scalaVersion() match {
      case "2.13.17" => Seq("-language:experimental.macros")
      case _         => Nil
    })

  def pomSettings = PomSettings(
    "extra",
    "com.github.japgolly.scalajs-react",
    "https://github.com/japgolly/scalajs-react",
    Seq(License(
      "",
      "Apache-2.0",
      "http://opensource.org/licenses/Apache-2.0",
      false,
      false,
      "repo"
    )),
    VersionControl(
      Some("https://github.com/japgolly/scalajs-react"),
      Some("scm:git:https://github.com/japgolly/scalajs-react.git"),
      Some("scm:git:git@github.com:japgolly/scalajs-react.git"),
      None
    ),
    Seq(Developer(
      "japgolly",
      "David Barri",
      "https://japgolly.github.io/japgolly/",
      None,
      None
    ))
  )

}
